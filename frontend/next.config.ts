import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["38.242.137.145","pin-waterproof-driven-municipality.trycloudflare.com"],
};

export default nextConfig;
