# Plan: Rename Faculty → Course (13 files, ~92 occurrences)

## Context
Backend API has been updated to use "Course" instead of "Faculty". Frontend must match.

## Critical API Path Changes
| Current (wrong) | New (correct) |
|-----------------|---------------|
| `/api/content/faculty` | `/api/content/course` |
| `/api/faculty/:id` | `/api/content/course/:id` |
| `/api/subject/faculty/:id` | `/api/content/subject/course/:id` |

## Critical Field Name Changes
| Current | New |
|---------|-----|
| `faculty_id` | `course_id` |
| `faculty: { id, name }` | `course: { id, name }` |
| `response.data.faculties` | `response.data.courses` |
| `studentsInFaculties` | `studentsInCourses` |
| `questionsInFaculties` | `questionsInCourses` |
| `questionsInEnrolledFaculties` | `questionsInEnrolledCourses` |

---

## Files to Modify

### 1. `lib/api-routes.ts`
- Rename `FACULTIES` → `COURSES`
- Fix endpoint paths to `/api/content/course`
- Fix `BY_FACULTY` → `BY_COURSE` with path `/api/content/subject/course/:courseId`

### 2. `lib/api.ts`
- Rename `Faculty` → `Course`
- Rename `FacultiesResponse` → `CoursesResponse`
- Rename `faculties` → `courses` in response type
- Update `Subject` interface: `faculty_id` → `course_id`, `faculty` → `course`
- Update `Topic` interface: `subject.faculty` → `subject.course`
- Rename `facultiesApi` → `coursesApi`
- Update dashboard stats fields: `studentsInFaculties` → `studentsInCourses`, etc.

### 3. `hooks/use-faculties.ts`
- Rename file to `hooks/use-courses.ts`
- Rename `UseFacultiesOptions` → `UseCoursesOptions`
- Rename `useFaculties` → `useCourses`
- Update all internal references

### 4. `hooks/use-delete-with-undo.ts`
- Change type from `"faculty"` to `"course"` in PendingDeletion and UseDeleteWithUndoOptions

### 5. `components/dialogs/add-faculty-dialog.tsx`
- Rename file to `components/dialogs/add-course-dialog.tsx`
- Rename `AddFacultyDialogProps` → `AddCourseDialogProps`
- Rename `AddFacultyDialog` → `AddCourseDialog`
- Update all UI text ("Add Faculty" → "Add Course", etc.)

### 6. `components/dialogs/add-subject-dialog.tsx`
- Update imports: `useFaculties` → `useCourses`
- Rename `facultyId` → `courseId`
- Rename `selectedFaculty` → `selectedCourse`
- Update UI labels ("Faculty *" → "Course *", "Select faculty" → "Select course")
- Update dialog description text

### 7. `app/(dashboard)/dashboard/faculties/page.tsx`
- Rename directory to `courses/`
- Rename file to `page.tsx`
- Update all imports and references
- Update UI text ("Faculties" → "Courses", "Add Faculty" → "Add Course")

### 8. `app/(dashboard)/dashboard/subjects/page.tsx`
- Update column header from "Faculty" to "Course"
- Update `s.faculty?.name` → `s.course?.name`
- Update description text

### 9. `app/(dashboard)/dashboard/topics/page.tsx`
- Update column header from "Faculty" to "Course"
- Update `t.subject?.faculty?.name` → `t.subject?.course?.name`

### 10. `app/(dashboard)/dashboard/page.tsx`
- Update stats card labels and values
- `studentsInFaculties` → `studentsInCourses`
- `questionsInFaculties` → `questionsInCourses`
- `questionsInEnrolledFaculties` → `questionsInEnrolledCourses`
- Update period labels ("Your Faculties" → "Your Courses")

### 11. `components/shadcn-space/blocks/sidebar-06/app-sidebar.tsx`
- Update nav item title: "Faculties" → "Courses"
- Update child link: "Faculties" → "Courses"
- Update href: `/dashboard/faculties` → `/dashboard/courses`

### 12. `PRD.md`
- Update all references from "Faculty" to "Course"

---

## Execution Order
1. Rename files first (hooks, dialogs, pages directory)
2. Update type definitions (api.ts)
3. Update API routes (api-routes.ts)
4. Update hooks
5. Update components (dialogs, sidebar)
6. Update pages
7. Update dashboard stats
8. Update documentation (PRD.md)

## Verification
1. Run `pnpm run build` to verify no type errors
2. Verify all routes work: `/dashboard/courses`, `/dashboard/subjects`, `/dashboard/topics`
3. Verify API calls hit correct endpoints
